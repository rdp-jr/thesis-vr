AFRAME.registerComponent('gameState', {
    schema: {
        isClickedRemote: {type: 'boolean', default: false}
    },

    init: function() {
        var data = this.data
        var el = this.el
    },

   
})